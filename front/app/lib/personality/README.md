# Personality UI

최초 선택지 입력, 성향 분석 흐름과 `성향 분석 다시하기` 진입 화면을 둔다.

신규 사용자는 서비스 설명이 포함된 닉네임 화면을 거쳐 선택폼에 진입한다. 선택폼 첫 문항에는 이전 버튼을 표시하지 않고 두 번째 문항부터 표시한다. 동일 브라우저·앱의 기존 사용자는 저장된 `userKey`로 자동 복원된다.

구형 `survey` 영역은 제거됐으며 선택지 원본 제출과 분석은 Personality API 하나를 사용한다.
성향 결과 프로필 화면 자체는 `lib/profile`이 담당한다.
