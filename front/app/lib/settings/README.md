# Settings

Novelty no longer exposes a user-facing mission settings screen.

The daily mission flow requests up to five candidates from the Backend and
allows the user to select one mission. Available time and daily mission count
are not sent or stored as user settings.
