프로젝트 명 : 노벨티  
어떤 일이나 물건에 새로운 요소를 더했을 때, 그 변화 자체에 흥미를 느껴 일시적으로 관심이나 성과가 좋아지는 현상인 노벨티 효과에서 착안함

설명
매일매일 자그마한 미션을 랜덤으로 부여하고 하게 만드는 형태의 서비스, 사용자의 성향을 파악하기 위해 초반 선택지 폼을 제공해 입력 받고 성향과 반대되는 활동을 미션으로 생성함, 사용자가 미션을 선택 후 수행하면 3D 공간이 성장하게 되는 요소를 가지고 있다. 

타겟  
새로운 활동으로 활력을 찾고 싶은 사람 

문제 상황   
권태, 무기력증, 우울감 개선

이유  
뭔가를 해야한다는 것을 느끼지만 실제로 벗어나기가 어려웠음, 새로운 활동에 대한 의지를 가질 때 도와줄 보조 수단이 있으면 좋을 거 같음 

효과  
행동활성화, 우울감 개선

차별화  
사용자가 미션을 달성할 시 미션 내용과 연관된 스티커를 제공하여 확장되는 세계를 보여줌 

주요기능  
랜덤한 미션 생성 후 선택지 제안
미션 달성을 통해 성장하는 3D 공간

사용할 기술
front
- flutter
- dart
- threeJS

backend
- java 21
- spring boot

db
- oracle 21c

[할일]
이 프로젝트를 구현하기 위한 최소한의 프로젝트 구조를 제안해줘
폴더 내에 각각 front , back이 있을 수 있도록.

[제약사항]
아직 기능을 구현하지 말고 구조만 제안할 것


---

방금 제안한 구조에서
이번 노벨티 프로젝트에 꼭 필요한 부분과
없어도 되는 부분을 구분해줘.

이번 목표는 다음 4가지 기능뿐이야.

1. 선택지 폼 제공
2. 사용자 성향 분석
3. 성향과 반대되는 랜덤한 미션 생성
4. 3D 구조의 공간과 레벨 시스템 

구조를 최대한 단순하게 유지해줘.

아직 파일은 생성하지 마.

---

지금 합의한 최소 구조를 기준으로
Frontend와 Backend 프로젝트 틀을 만들어줘.

조건:

1. 아직 기능은 구현하지 않는다.
2. Frontend와 Backend가 각각 실행되는 상태까지만 만든다.
3. 불필요한 Library를 추가하지 않는다.
4. 생성한 파일과 사용한 명령어를 마지막에 정리한다.
5. 실행에 실패하면 숨기지 말고 오류를 알려준다.

---
application.yml 파일에 DB 정보를 추가해.
오라클 접속정보는 로컬호스트를 사용하고, SID는 XE, 포트는 1521이고 아이디는 C##LG_DB , 패스워드는 12345이야. 값에 맞게 파일 내용 업데이트해.

---

현재 프로젝트를 다시 분석해줘.

아직 코드는 수정하지 마.

다음 내용을 실제 파일을 기준으로 설명해줘.

1. Frontend 프로젝트 위치
2. Backend 프로젝트 위치
3. Frontend 실행 방법
4. Backend 실행 방법
5. Spring Boot 시작 Class
6. 앞으로 Controller를 작성할 위치
7. Database 관련 설정 위치
8. Flutter 첫 화면 위치

관련 파일 경로도 같이 알려줘.

---

현재 프로젝트를 기준으로
AGENTS.md 초안을 작성해줘.

다음 내용은 반드시 포함해줘.

1. 프로젝트 목적
2. Frontend 기술
3. Backend 기술
4. Backend 기본 구조
5. REST API 사용
6. 불필요한 Library 추가 금지
7. Secret을 코드에 직접 작성하지 않기
8. 기존 파일을 대량으로 삭제하지 않기
9. 큰 변경 전에 계획 먼저 설명하기
10. 구현 후 Build 또는 테스트하기
11. 변경한 파일을 작업 마지막에 설명하기

아직 파일을 생성하지 말고 먼저 내용을 보여줘.

---

위 내용에 맞춰서 AGENTS.md 파일을 생성해줘

---

사용자에게 어떤 선택지 폼을 제공하면 성향을 파악할 수 있지만 번거롭지않고 이탈율이 적은 형태로 만들 수 있을까?

---

이제 첫 번째 핵심 기능으로 선택지 폼 생성을 구현할거야.
위에서 너가 답변한 내용을 토대로
최종적으로 사용자가 화면에서 선택지를 다 고르면
Oracle Database에 데이터가 저장되어야해.

현재 프로젝트 구조와 AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---

그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7을 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

----
phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
sdd에 정의된 내용을 토대로 반복 진행 
---

지금까지 구현한 내용을 실행해서 볼 수 있게 어디서 어떻게 실행하면 되는지 알려줘.

---

지금까지 구현한 내용을 적절히 기능별로 분리해서 commit하고 push해줘.
커밋메세지는 한글로 작성해.

---

사용한 API를 swaggerUI를 통해 직접 테스트할 수 있게 연동해주고 테스트할 수 있는 로컬 호스트 주소로 알려줘. 그리고 앞으로 추가될 API도 동일하게 적용해.

---

프로젝트에 필요한 DB는 너가 직접 테이블을 생성해서 적용하도록 하고 지금까지 작성했던 내용과 앞으로 실행할 내용에 대해서도 DB.sql 파일에 동일하게 작성해줘.

---
앞으로 모든 디자인은 DESIGN.md를 참고해서 디자인 스타일을 적용하고 이전에 생성한 내용들도 같이 수정해.
사용할 폰트는 NotoSans-kr이야. 구글폰트에서 Noto Sans Korean 로 찾아서 적용해.

---

이제 두 번째 핵심 기능으로 사용자 성향분석을 구현 할거야.
앞서 구현한 선택지 폼에 대한 내용과 연계하여 사용자가 고른 선택지 답에 따라 성향이 정해질거야.성향 유형을 분류하고 네이밍을 해줘. 여기서 정해진 성향은 추후 랜덤 미션 생성에 사용이 될거야. 이 부분도 고려해.

사용자 성향에 대한 정보는 Oracle Database에 유저 정보로 데이터가 저장되어야해. 외에 내가 언급하지 않았지만 필요한 정보가 있다면 저장해서 관리해.

현재 프로젝트 구조와 AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---

그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7을 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

----

성향 분석을 매번 진행하는게 아닌 최초 1회 실행 하고 사용가능한 시간을 묻는 질문은 성향 분석이 끝난 후에 묻는 형태로 바꿔줘. 사용가능 시간을 묻는 질문 대신 다른 성향을 파악 할 수 있는 질문으로 변경, 앞의 내용과 중복되지 않은 새로운 유형을 분석할 수 있는 질문으로 선정해줘.

[사용자 플로우]
접속 - 최초 1회 성향 분석 선택폼 진행 - 성향 결과 + 할애 가능시간 질문 진행 - 종합 결과로 랜덤 미션 생성

추가적으로 사용자가 성향 분석을 다시 진행하고 싶다면 성향 분석 다시하기 버튼을 제공해서 진행

사용자 성향 분석 결과가 나왔을 때 사용자 성향을 프로필로 보여주기
사용자 회원가입 없이 닉네임 설정해서 로그인하고 브라우저 캐시 또는 앱 캐시로 사용자 정보 저장
닉네임은 초기 랜덤값 부여하고 사용자가 변경할 수 있도록 기능 추가
닉네임 초기값은 노벨티+숫자2자리+영문2자리 랜덤 조합으로 중복 없이 생성 
닉네임 정책으로는 중복 불가, 공백 불가,특수문자 불가, 최소 1글자 이상, 비속어 불가 

----

수정된 SSD를 적용하기 위해서 사전 설정해야하는 일이 있다면 알려줘

----

phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
...
phase 2에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
sdd에 정의된 내용을 토대로 반복 진행 
----

닉네임 관련해서 금칙어 목록 파일을 하나 생성해서 3단 구조로 관리하게 해줘
Frontend validation
+
Backend validation
+
DB constraint

----

서비스의 날짜 기준 timezone은 Asia/Seoul로 해.
사용자 식별을 위해 userID를 부여해서 관리해.
최근 완료 미션 3일간 재추천 금지
최근 노출 미션 2일간 재노출 금지
동일 카테고리 연속 추천 제한
미션 상태 로그 종류 변경 기존 선택,완료에서 GENERATED, SHOWN, SELECTED, CANCELLED, COMPLETED 로 변경, UI로 표시되는 내용은 SELECTED일 때 수행중, COMPLETED 일 때 완료로 표기  

----
사용자 성향을 고려해서 사용자가 많이 해보지 않았을 법한 행동을 미션 내용으로 생성하라고 했는데 이걸 구현할 때는 
미션에도 사용자 성향 벡터에서 사용한 값을 부여해서 사용자의 성향과 얼마나 거리가 먼지 계산 할 수 있게 해줘. 

미션 속성 추가 

id
title
description

category
difficulty

estimatedMinutes

indoorOutdoor
socialLevel
activityLevel
noveltyLevel

enabled

최초 성향 분석 후 미션 생성 시에는 기본적으로 미리 만들어둔 미션 내용 중에서 선별해서 사용해. 
5회 이상 미션을 수행해서 성향이 업데이트 된 사용자에 한해 Open AI LLM을 이용해서 추가 미션 내용을 생성하고 DB에 추가해서 다른 유사한 사용자에게도 해당 미션이 뜨게 관리해.
LLM이 미션을 새로 생성하는 주기는 미션 수행 5회를 기준으로 업데이트하게 해.

미션 생성 시에는 기존에 존재하던 미션과 중복되지 않아야하고 너무 유사하면 안돼.

----

지금까지 전체 구현 된 내용을 기준으로 정책 상 오류가 있거나 구현이 잘못된 부분은 없는지 확인하고 아직 코드는 수정하지마.

----
전체 기술 구조를 아래와 같은 형태로 맞춰.
Novelty

Frontend
│
├─ Flutter
│   ├─ 선택지 폼
│   ├─ 성향 분석 UI
│   ├─ 미션 UI
│   ├─ 프로필
│   ├─ 설정
│   └─ 3D World 화면
│
└─ Three.js
    └─ 3D World Renderer
        ├─ GLB Load
        ├─ Camera
        ├─ Lighting
        ├─ Object placement
        ├─ Level model switching
        └─ Simple animation


Backend
│
└─ Spring Boot
    ├─ User
    ├─ Personality
    ├─ Mission
    ├─ Mission Completion
    └─ World / Progression


Database
│
└─ Oracle
    ├─ USER
    ├─ PERSONALITY
    ├─ MISSION
    ├─ USER_MISSION
    ├─ WORLD_OBJECT
    ├─ WORLD_OBJECT_LEVEL
    └─ USER_WORLD_OBJECT


3D Assets

Blender / Asset 제작
↓
GLB
↓
Flutter bundle 또는 CDN
↓
Three.js

----

PROJECT_STATUS.md를 하나 만들고 핵심 기능 별로 SDD 진행상태에 대해 관리해서 혼선을 일으키지 않도록 해.
SPEC.md를 생성해서 현재까지 정의된 프로젝트 내의 정책과 규칙들을 정리하고 새로운 내용을 구현하기 전에 AGENT.md, SDD Phase와 더불어 기존 프로젝트가 어떻게 짜여져있는지 참고하게 해. 새로운 규칙이나 스펙이 정의되면 spec.md에 업데이트해.

----

두 번째 핵심 기능 SDD에 따라 구현 도중에 많은 내용이 변경 되었기 때문에 두번째 핵심 기능에 대한 SDD를 재정의하고 다시 설계해줘. 이번 단계에서 미션생성에 대한 내용은 제외하고 성향 분석에 대해서만 진행해. 성향 분석 단계가 끝나고 나서 미션에 대한 기능 구현을 진행할거야.

----

SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑하고 난 후 
phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.

----

지금까지 구현한 내용을 적절히 기능별로 분리해서 commit하고 push해줘.
커밋메세지는 한글로 작성해.

-----

이제 세 번째 핵심 기능으로 성향과 거리가 먼 랜덤한 미션 생성을 구현 할거야.
사용자 성향을 고려해서 사용자가 많이 해보지 않았을 법한 행동을 미션 내용으로 생성해줘. 

사용자가 여러개의 미션중 하나를 선택해서 수행할 수 있는 형태로 만들어. 
하루에 수행하는 미션은 한개로 고정해. 따로 설정을 제공해서 수행할 미션 수를 선택할 수 있게 해줘. 

생성되는 미션은 큰 카테고리로 종류를 분류할 수 있게 해, 수행한 미션의 카테고리에 따라 추후 3D 공간에 배치된 물품이 성장하게 될거야. 
예를 들면 운동 , 음식 , 문화 등 앞서 선택지 폼에서 물어본 활동 같이 말이야. 

여러개 미션 목록에서 선택한 미션은 취소하거나 변경할 수 있어. 선택해서 수행중인 미션만 상단에 보이게 하고 변경 버튼을 두어 기존 미션 목록에서 다른 미션으로 바꿀 수 있게 해. 

사용자가 수행한 미션 내용에 대한 정보를 저장하고 성향에 업데이트를 적용해서 어떤 카테고리의 미션을 주로 수행했는지에 대한 정보를 추가해줘.

생성된 미션에 대한 정보와 수행여부, 미션상태는 Oracle Database에 데이터가 저장되어야해. 외에 내가 언급하지 않았지만 필요한 정보가 있다면 저장해서 관리해.

현재 프로젝트 구조와 spec.md, AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---


그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

---- 여기까지 26.08.19 진행 

phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
sdd에 정의된 내용을 토대로 반복 진행 

---

현재까지 구현된 노벨티 프로젝트를 전체적으로 점검해줘.

아직 코드를 수정하지 마.

AGENTS.md, DESIGN.md, 각 기능의 SDD, DB.sql과 실제 Frontend/Backend 코드를 모두 확인한 뒤 분석해.

현재까지 구현 대상은 다음 3개 핵심 기능이야.

1. 선택지 폼
2. 사용자 성향 분석
3. 사용자 성향을 기반으로 한 미션 추천/선택/완료

다음 단계에서는 4번째 핵심 기능인 3D 공간 및 레벨 시스템을 구현할 예정이야.

아래 항목을 기준으로 현재 구조에 문제가 없는지 점검해줘.

1. 현재 구현된 기능과 미구현 기능 구분
2. SDD와 실제 구현의 차이
3. Frontend ↔ REST API ↔ Spring Boot ↔ Oracle 연결 상태
4. 사용하지 않는 Mock 데이터 또는 임시 코드 존재 여부
5. 중복 코드 또는 불필요하게 복잡한 구조
6. API Request/Response 구조의 일관성
7. 사용자 식별 방식
8. 닉네임 중복 방지 방식
9. 성향 분석 결과 저장 구조
10. 미션 추천 로직과 성향 데이터 연결 구조
11. 미션 상태 저장 구조
12. 미션 중복 추천 방지 여부
13. 하루 미션 기준 및 timezone 처리
14. 미션 카테고리와 향후 3D 공간 성장 시스템을 연결하기에 적절한 데이터 구조인지
15. DB constraint 및 데이터 무결성
16. 예외 처리
17. 앱 재실행 또는 브라우저 재접속 시 사용자 상태 복원
18. Swagger에서 현재 모든 API 테스트 가능 여부
19. Backend build/test 상태
20. Frontend build/analyze 상태

각 문제에 대해서는 아래 기준으로 분류해줘.

* 반드시 지금 수정해야 함
* 3D 기능 구현 전에 수정해야 함
* 나중에 개선 가능
* 현재 문제 없음

특히 현재 기능이 정상 동작하더라도 앞으로 3D 공간 및 레벨 시스템을 구현할 때 구조 변경을 크게 유발할 가능성이 있는 부분을 우선적으로 찾아줘.

아직 어떤 파일도 수정하지 말고 분석 결과와 수정 권장 순서만 알려줘.

-----



-----
현재 노벨티 프로젝트의 4번째 핵심 기능인 **3D 공간 및 레벨 시스템**을 설계하려고 해.

아직 코드를 수정하거나 파일을 생성하지 마.

먼저 현재 프로젝트의 다음 내용을 실제 파일 기준으로 확인해.

* AGENTS.md
* DESIGN.md
* 기존 SDD 문서
* Frontend 구조
* Backend 구조
* DB.sql
* 현재 사용자/성향/미션 관련 Entity, Service, Controller, Repository
* 현재 API 구조
* 현재 Flutter 상태 관리 및 화면 구조

이번 단계에서는 기존 구조를 최대한 유지하면서 3D 공간 기능을 추가할 수 있는 아키텍처를 설계해.

3D 공간은 본디와 유사한 **고정된 아이소메트릭 형태의 방**을 기본으로 한다.

캐릭터 이동이나 물리 시스템은 현재 범위에 포함하지 않는다.

공간에는 미션 카테고리와 연결된 기물이 배치되어 있고, 사용자가 해당 카테고리의 미션을 수행할 때마다 경험치가 누적되며 해당 기물의 외형이 단계별로 성장하는 구조로 구현할 예정이다.

예:

```text
운동 미션 완료
→ 운동 카테고리 EXP 증가
→ 운동 관련 World Object 레벨 증가
→ 해당 Object의 GLB Asset 교체
```

전체적인 구조는 아래 책임 분리를 기준으로 검토해.

```text
Flutter
= 앱 UI / 사용자 상태 / API 통신

Three.js
= 3D World Renderer

Spring Boot
= 미션 완료 / EXP / Level / World Progress 등 비즈니스 규칙

Oracle
= 사용자와 World 상태 저장

GLB
= 3D Asset
```

특히 아래 요구사항을 모두 고려해서 SDD 기반으로 설계해.

---

## 1. Flutter와 Three.js 역할 분리

Flutter 내부에서 Three.js를 직접 앱 전체 로직과 섞지 말고, Three.js를 **3D World 전용 Renderer**로 분리하는 구조를 설계해.

기본 데이터 흐름은 다음 구조를 우선적으로 검토해.

```text
Spring Boot
    ↓
Flutter
    ↓
Three.js Renderer
```

Three.js가 Spring Boot API를 직접 호출하지 않도록 해.

Three.js는 서버 상태를 관리하지 않고 Flutter가 전달한 World 상태를 시각적으로 렌더링하는 역할만 담당하게 해.

Flutter와 Three.js 사이에서 어떤 데이터와 명령을 전달해야 하는지도 정의해.

예:

```text
updateWorld(worldState)
updateObjectLevel(objectCode, level)
playLevelUpAnimation(objectCode)
```

단, 실제 함수명은 현재 프로젝트 스타일에 맞게 설계해.

---

## 2. World 상태 API 데이터 구조

3D 공간 화면을 열었을 때 Flutter가 Backend에서 전체 World 상태를 한 번에 받을 수 있는 Snapshot 형태의 API를 설계해.

예시 형태:

```http
GET /api/world
```

Response에는 최소한 아래 정보가 포함될 수 있도록 검토해.

```json
{
  "worldLevel": 3,
  "objects": [
    {
      "objectCode": "FITNESS_PLANT",
      "category": "FITNESS",
      "level": 3,
      "exp": 74
    },
    {
      "objectCode": "CULTURE_BOOKSHELF",
      "category": "CULTURE",
      "level": 4,
      "exp": 135
    }
  ]
}
```

현재 프로젝트의 기존 API Response 규칙이 있다면 해당 규칙을 우선 사용해.

API 구조를 불필요하게 복잡하게 만들지 마.

---

## 3. DB와 3D Asset 직접 연결 금지

Oracle Database에서 GLB 파일 경로나 실제 Asset 파일 이름을 직접 관리하지 않는 구조를 설계해.

아래와 같은 직접 연결은 피한다.

```text
DB
CULTURE
→ assets/world/bookshelf_lv3.glb
```

대신 Database에서는 의미 있는 식별자와 상태만 관리해.

예:

```text
objectCode = CULTURE_BOOKSHELF
level = 3
```

실제 Asset 경로는 Frontend의 Asset Manifest 또는 Three.js 전용 설정 파일에서 매핑하도록 해.

예:

```json
{
  "CULTURE_BOOKSHELF": {
    "1": "assets/world/culture/bookshelf/bookshelf_lv01.glb",
    "2": "assets/world/culture/bookshelf/bookshelf_lv02.glb",
    "3": "assets/world/culture/bookshelf/bookshelf_lv03.glb"
  }
}
```

Asset 경로 변경이 Backend나 DB 변경으로 이어지지 않게 구조를 분리해.

---

## 4. Mission → Category → World Object 구조

Mission과 실제 GLB Asset을 직접 연결하지 마.

아래 구조를 기본으로 사용해.

```text
Mission
    ↓
Mission Category
    ↓
World Object
    ↓
World Object Level
    ↓
Asset Key
    ↓
GLB
```

예:

```text
15분 산책하기

↓

FITNESS

↓

FITNESS_PLANT

↓

Level 3

↓

fitness_plant_lv03.glb
```

Mission Category와 World Object 사이의 관계를 DB에서 어떻게 관리하는 것이 가장 단순하고 확장성이 좋은지도 설계해.

---

## 5. 초기 World Object 개수 제한

MVP에서는 지나치게 많은 3D Object를 만들지 않는다.

Mission Category당 대표 World Object 하나 정도를 우선 고려해.

예:

| Mission Category | World Object 예시  |
| ---------------- | ---------------- |
| FITNESS          | 식물 / 운동 관련 오브젝트  |
| FOOD             | 식탁 / 주방 오브젝트     |
| CULTURE          | 책장               |
| OUTDOOR          | 창문 / 테라스 관련 오브젝트 |
| SOCIAL           | 소파               |
| HOBBY            | 작업 책상            |

실제 프로젝트의 현재 Mission Category를 확인한 후 적절한 매핑안을 제시해.

존재하지 않는 Mission Category를 임의로 추가하지 말고 필요하다면 추가 후보로만 제안해.

---

## 6. Object 성장 방식

미션 수행마다 새로운 Object를 무한히 추가하는 구조 대신 하나의 대표 Object가 Level에 따라 다른 외형으로 교체되는 방식을 우선 사용해.

예:

```text
Bookshelf Lv1
Bookshelf Lv2
Bookshelf Lv3
Bookshelf Lv4
Bookshelf Lv5
```

Level별 GLB Asset을 준비하고 사용자 Level에 따라 해당 Asset을 로드하는 구조로 설계해.

각 Object는 기본적으로 5단계 정도를 가정하되 추후 Level 수가 달라져도 구조 변경이 크지 않도록 해.

---

## 7. EXP와 Level 분리

Mission 완료 횟수를 그대로 Object Level로 사용하지 말고 EXP 시스템을 사용해.

예:

```text
Easy Mission
→ 10 EXP

Normal Mission
→ 20 EXP

Challenge Mission
→ 30 EXP
```

현재 Mission 구조에 난이도 개념이 있다면 기존 값을 활용하고, 없다면 최소한의 추가 방식만 제안해.

Mission에는 필요하다면 아래 값을 추가할 수 있도록 검토해.

```text
rewardExp
```

Level 계산은 Backend에서 수행해.

Flutter나 Three.js에서 EXP 또는 Level을 계산하지 마.

---

## 8. World Object 관련 DB 구조

현재 DB 구조를 확인한 후 최소한 다음 개념이 필요한지 검토해.

### WORLD_OBJECT

World Object 기본 정보.

예:

```text
id
object_code
category_code
name
max_level
enabled
```

### WORLD_OBJECT_LEVEL

Level 기준 정보.

예:

```text
id
world_object_id
level
required_exp
```

GLB 경로는 이 테이블에 저장하지 않는 것을 우선으로 해.

### USER_WORLD_OBJECT

사용자별 World Object 성장 정보.

예:

```text
id
user_id
world_object_id
exp
level
updated_at
```

현재 DB 구조와 중복되는 Entity 또는 Table이 있다면 새로 만들지 말고 기존 구조를 활용하는 방향을 먼저 검토해.

필요한 PK, FK, UNIQUE Constraint도 함께 설계해.

---

## 9. Mission 완료와 World Progress 연결

Mission 완료 시 아래 흐름이 하나의 Backend 비즈니스 로직으로 연결되도록 설계해.

```text
Mission 완료
    ↓
USER_MISSION 상태 COMPLETED
    ↓
Mission Category 확인
    ↓
해당 World Object 확인
    ↓
EXP 지급
    ↓
현재 EXP 갱신
    ↓
Level 재계산
    ↓
USER_WORLD_OBJECT 갱신
```

필요하다면 아래와 같은 Service 책임 분리를 검토해.

```text
MissionCompletionService

WorldProgressService
```

다만 현재 프로젝트 규모에 비해 과도한 Service 분리는 하지 마.

---

## 10. Mission 완료 API Response

Mission 완료 시 단순히 완료 여부만 반환하지 말고 World가 성장했는지 Flutter에서 즉시 알 수 있도록 Response 구조를 설계해.

예:

```json
{
  "missionStatus": "COMPLETED",
  "worldProgress": {
    "objectCode": "FITNESS_PLANT",
    "previousLevel": 2,
    "currentLevel": 3,
    "exp": 70,
    "levelUp": true
  }
}
```

기존 프로젝트의 공통 Response 규칙이 있다면 해당 규칙을 따른다.

Flutter는 이 결과를 이용해 Level Up UI를 보여주고 Three.js Renderer에 Object 변경을 전달하게 해.

---

## 11. Mission 완료 중복 요청 방지

Mission 완료 API가 여러 번 호출되더라도 EXP가 중복 지급되지 않도록 설계해.

예:

```text
첫 번째 완료 요청

MISSION
SELECTED → COMPLETED

EXP +20
```

이후 동일 요청:

```text
이미 COMPLETED

→ 추가 EXP 지급 금지
```

Mission 상태 변경과 EXP 지급은 가능한 한 동일한 Transaction 범위에서 처리하도록 해.

동시 요청이 들어와도 중복 보상이 발생하지 않도록 DB Constraint 또는 Transaction 처리 방법을 검토해.

---

## 12. Asset 관리 구조

3D Runtime Asset은 FBX 대신 GLB를 기본 포맷으로 사용하도록 설계해.

Asset 제작 흐름은 다음 형태를 기본으로 해.

```text
Blender
↓
GLB Export
↓
Asset Optimization
↓
Frontend Asset
↓
Three.js GLTFLoader
```

초기 프로젝트에서는 GLB 파일을 Flutter 프로젝트 또는 Three.js Renderer의 Asset 디렉터리에 포함시키는 구조를 사용해.

추후 서비스 확장 시 CDN 또는 Object Storage로 이전할 수 있게 Asset 경로와 비즈니스 로직을 분리해.

Oracle Database에 GLB 파일을 BLOB 형태로 저장하지 마.

---

## 13. Asset 디렉터리와 Naming Convention

3D Asset 관리가 혼란스럽지 않게 Asset 폴더 구조와 파일 Naming Rule을 설계해.

예:

```text
assets/world/

room/
  room_base.glb

objects/

  fitness/
    plant/
      plant_lv01.glb
      plant_lv02.glb
      plant_lv03.glb
      plant_lv04.glb
      plant_lv05.glb

  culture/
    bookshelf/
      bookshelf_lv01.glb
      bookshelf_lv02.glb
      bookshelf_lv03.glb
      bookshelf_lv04.glb
      bookshelf_lv05.glb
```

아래 규칙도 정의해.

* Level 번호 표현
* category 이름
* object 이름
* 파일명 소문자/대문자 정책
* 공백 사용 여부
* Asset Key와 파일 이름의 관계
* Room Asset과 Object Asset 구분

---

## 14. Asset 제작 규칙

향후 Blender 또는 외부 3D Asset을 추가할 때 문제가 생기지 않도록 최소한의 3D Asset 제작 규칙도 정의해.

예:

```text
Unit
1 = 1 meter

Pivot
Object 하단 또는 배치 기준점

Forward Axis
프로젝트 전체 통일

Texture
512 또는 1024 우선

Material
가능하면 단순한 PBR

Polygon
모바일 환경을 고려한 Low Poly 우선
```

특히 아래 문제를 예방할 수 있게 해.

* 모델마다 Scale이 크게 다름
* Object가 바닥에서 뜸
* Rotation 방향이 다름
* Texture 크기가 지나치게 큼
* Material이 Three.js에서 다르게 보임
* 한 Object 때문에 Draw Call이나 GPU 부하가 지나치게 증가

필요하다면 별도의 ASSET_GUIDE.md를 만드는 방안을 제안할 수 있지만 아직 파일은 생성하지 마.

---

## 15. Object Position 관리

현재 범위에서는 사용자가 가구 위치를 자유롭게 배치하는 기능은 만들지 않는다.

따라서 Object의 Position, Rotation, Scale은 DB에 저장하지 않는 방향을 우선 검토해.

고정 배치 데이터는 Frontend의 World Manifest에서 관리해.

예:

```json
{
  "CULTURE_BOOKSHELF": {
    "position": [-2, 0, -3],
    "rotation": [0, 1.57, 0],
    "scale": [1, 1, 1]
  }
}
```

향후 사용자가 직접 가구를 이동하는 기능이 추가되면 그때 USER_WORLD_OBJECT에 위치 데이터를 저장할 수 있도록 확장 가능성만 고려해.

현재는 해당 컬럼을 미리 추가하지 마.

---

## 16. Camera 및 3D 공간 표현 방식

3D 공간은 본디와 유사한 **아이소메트릭 디오라마 공간**을 목표로 한다.

자유로운 1인칭 또는 3인칭 Camera는 사용하지 않는다.

다음 방식 중 어떤 것이 현재 프로젝트에 적합한지 검토해.

```text
Orthographic Camera

또는

제한된 Perspective Camera
```

사용자 Interaction은 MVP 기준 아래 정도만 고려해.

```text
Drag
→ 제한적인 Camera 회전

Pinch / Wheel
→ Zoom

Object Tap
→ Object 정보 확인
```

Camera 이동 범위와 Zoom 범위를 제한해서 항상 방 전체의 구성이 유지될 수 있도록 설계해.

복잡한 Character Controller, Physics, NavMesh는 구현 범위에서 제외해.

---

## 17. Flutter 화면과 3D Renderer UI 책임

Three.js에서 앱 UI까지 그리지 마.

앱의 UI 요소는 Flutter가 담당하도록 해.

예:

```text
Flutter

- AppBar
- World Level
- Category Level
- Level Up 메시지
- Mission 완료 결과
- Object 상세 정보 UI

Three.js

- Room
- World Object
- Camera
- Lighting
- Shadow
- Object Level 변경
- 간단한 Level Up Animation
```

Three.js Canvas 또는 WebView 위에 Flutter UI가 배치되는 구조를 검토해.

---

## 18. 3D Technical Spike를 먼저 설계

전체 3D World 기능 구현 전에 별도의 **Technical Spike** 단계를 만들어.

목적은 Flutter + Three.js + GLB 조합이 실제 프로젝트 환경에서 정상적으로 작동하는지 검증하는 것이다.

이 단계에서는 실제 Mission, DB, World API와 연결하지 않는다.

최소 검증 범위는 다음과 같다.

```text
Flutter World Test Screen

↓

Three.js Renderer 실행

↓

room_base.glb 로드

+

test_object_lv01.glb 로드
```

그리고 테스트용 버튼 하나를 Flutter에 둔다.

```text
[LEVEL UP]
```

버튼 클릭 시 Flutter → Three.js 통신을 통해

```text
test_object_lv01.glb

↓

test_object_lv02.glb
```

로 교체되는지 확인해.

Technical Spike에서는 최소한 아래를 검증해.

1. Flutter에서 Three.js Renderer 정상 로드
2. GLB 정상 로드
3. Camera 정상 렌더링
4. Light / Shadow 정상 동작
5. Flutter → JavaScript 통신
6. Three.js Object 교체
7. 모바일 또는 현재 타겟 환경에서 FPS 및 메모리 문제 여부
8. 화면 종료 후 다시 진입했을 때 Renderer가 정상적으로 Dispose / 재생성되는지
9. Flutter Hot Restart 또는 앱 재실행 시 치명적인 오류가 없는지
10. WebView를 사용한다면 WebView lifecycle 문제 여부

Technical Spike에 실패하면 실제 World 시스템 구현으로 넘어가지 말고 원인을 먼저 정리하도록 해.

---

## 19. 전체 기술 아키텍처 최종 검토

현재 프로젝트의 목표를 고려했을 때 아래 구조가 실제로 적합한지 마지막에 다시 평가해.

```text
Flutter
    ↓
Three.js Renderer
    ↓
GLB

Flutter
    ↓
REST API
    ↓
Spring Boot
    ↓
Oracle
```

특히 Flutter + Three.js 구현 방식에 대해 다음을 비교해.

### 후보 A

Flutter + WebView + Three.js

### 후보 B

Flutter Native 3D Library

### 후보 C

Unity 전체 앱

### 후보 D

Flutter + Embedded Unity

현재 노벨티의 범위:

* 캐릭터 없음
* 캐릭터 이동 없음
* Physics 없음
* 고정된 3D 방
* 제한된 Camera
* Category별 Object
* Object Level에 따른 외형 변경
* REST API 기반 앱
* 설문/프로필/미션 UI 비중이 큼

을 기준으로 가장 적합한 방식을 하나 추천해.

현재 선택한 Flutter + Three.js가 부적절하다고 판단된다면 숨기지 말고 이유와 대안을 설명해.

---

# 구현하지 말아야 할 것

이번 단계에서는 아래 작업을 하지 마.

* 실제 3D 기능 구현
* DB Table 생성
* Entity 생성
* API 구현
* GLB 생성
* Asset 다운로드
* Package 추가
* 기존 코드 수정
* 파일 생성

이번 단계의 목적은 **SDD 기반 설계와 구현 순서 결정**이다.

---

# 반드시 지켜야 할 아키텍처 원칙

아래 원칙을 기준으로 설계해.

```text
1. Three.js에서 사용자 성장 로직을 계산하지 않는다.

2. Flutter에서 EXP 또는 Level을 계산하지 않는다.

3. Three.js가 Backend API를 직접 호출하지 않는다.

4. Oracle Database에 GLB Binary를 저장하지 않는다.

5. Mission과 GLB 파일을 직접 연결하지 않는다.

6. Asset 경로를 Backend 비즈니스 로직에 직접 작성하지 않는다.

7. 사용자 World 상태는 Backend와 Database를 Source of Truth로 사용한다.

8. 3D Renderer는 전달받은 World 상태를 시각적으로 표현하는 역할만 담당한다.

9. 기존 기능과 기존 DB 구조를 가능한 한 재사용한다.

10. 현재 MVP에 필요하지 않은 확장 기능은 미리 구현하지 않는다.
```

---

# 최종 결과 작성 형식

분석 후 아래 순서로 결과를 작성해.

## 1. 현재 프로젝트 구조 분석

* 기존 Frontend
* 기존 Backend
* 기존 DB
* 기존 Mission Category
* 기존 Mission 완료 처리
* 3D 기능과 연결 가능한 기존 구조

## 2. 권장 기술 스택

각 기술의 역할과 선택 이유를 설명해.

```text
Flutter
Three.js
WebView 또는 선택한 연동 기술
Spring Boot
Oracle
GLB
Blender
```

## 3. 전체 Architecture

아래 관계를 포함한 구조를 보여줘.

```text
Flutter
→ REST API
→ Spring Boot
→ Oracle

Flutter
→ Three.js
→ GLB
```

## 4. 데이터 모델

필요한 Table과 관계를 설명해.

특히:

```text
MISSION
MISSION_CATEGORY
WORLD_OBJECT
WORLD_OBJECT_LEVEL
USER_WORLD_OBJECT
```

의 관계를 보여줘.

## 5. Mission → 3D 성장 데이터 흐름

예:

```text
Mission 완료
→ Category
→ EXP
→ World Object
→ Level
→ Asset Key
→ GLB
```

## 6. API 설계

필요한 API 후보와 Request / Response를 설계해.

기존 Mission 완료 API를 수정해야 하는지 신규 API가 필요한지도 구분해.

## 7. Asset 구조

* GLB 관리
* Asset Manifest
* World Manifest
* Directory
* Naming Convention
* Asset 제작 규칙

## 8. Flutter ↔ Three.js 통신

Flutter가 Three.js에 전달해야 하는 데이터와 Three.js가 Flutter에 반환해야 하는 이벤트를 정의해.

## 9. Technical Spike 계획

실제 3D World 개발 전에 최소한으로 검증할 Prototype의 구현 범위와 Acceptance Criteria를 작성해.

## 10. Phase 계획

실제 구현을 가능한 작은 단위의 Phase로 나눠.

예시는 참고만 하고 실제 프로젝트를 분석해서 적절히 조정해.

```text
Phase 0
Architecture / 기존 구조 정리

Phase 1
3D Technical Spike

Phase 2
World DB Schema

Phase 3
World Backend Domain

Phase 4
World API

Phase 5
Flutter World State

Phase 6
Three.js World Renderer

Phase 7
Mission Completion ↔ World Progress 연결

Phase 8
Level Up UI / Animation

Phase 9
Integration / Regression Test
```

## 11. 각 Phase Acceptance Criteria

각 Phase가 완료됐다고 판단할 수 있는 객관적인 조건을 작성해.

## 12. 구현 예상 파일

각 Phase별로 생성하거나 수정할 가능성이 있는 파일을 실제 현재 프로젝트 경로 기준으로 정리해.

## 13. 사전에 사용자가 준비해야 하는 사항

예:

* Blender 설치 여부
* 테스트용 GLB
* WebView 환경
* Android/iOS/Web Target
* 필요한 로컬 실행 설정

자동으로 처리 가능한 사항과 내가 직접 해야 할 일을 구분해.

## 14. 위험 요소

특히 아래 부분을 점검해.

* Flutter + Three.js 연동
* WebView lifecycle
* GLB 크기
* 모바일 GPU 성능
* Asset Loading
* Mission 중복 완료
* EXP 중복 지급
* DB Transaction
* 기존 Mission 기능 Regression
* Object Level 변경 시 Asset Loading Delay

## 15. 최종 권장 개발 순서

현재 코드 변경량과 위험도를 고려해서 가장 안전한 구현 순서를 제안해.

아직 구현은 시작하지 마.



