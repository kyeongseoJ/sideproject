프로젝트 명 : 노벨티  
어떤 일이나 물건에 새로운 요소를 더했을 때, 그 변화 자체에 흥미를 느껴 일시적으로 관심이나 성과가 좋아지는 현상인 노벨티 효과에서 착안함

설명
매일매일 자그마한 미션을 랜덤으로 부여하고 하게 만드는 형태의 서비스, 사용자의 성향을 파악하기 위해 초반 선택지 폼을 제공해 입력 받고 성향과 반대되는 활동을 미션으로 생성함, 사용자가 미션을 선택 후 수행하면 3D 공간이 성장하게 되는 요소를 가지고 있다. 

타겟  
새로운 활동으로 활력을 찾고 싶은 사람 

문제 상황   
권태, 무기력증, 우울감 개선

이유  
뭔가를 해야한다는 것을 느끼지만 실제로 벗어나기가 어려웠음, 새로운 활동에 대한 의지를 가질 때 도와줄 보조 수단이 있으면 좋을 거 같음 

효과  
행동활성화, 우울감 개선

차별화  
사용자가 미션을 달성할 시 미션 내용과 연관된 스티커를 제공하여 확장되는 세계를 보여줌 

주요기능  
랜덤한 미션 생성 후 선택지 제안
미션 달성을 통해 성장하는 3D 공간

사용할 기술
front
- flutter
- dart
- threeJS

backend
- java 21
- spring boot

db
- oracle 21c

[할일]
이 프로젝트를 구현하기 위한 최소한의 프로젝트 구조를 제안해줘
폴더 내에 각각 front , back이 있을 수 있도록.

[제약사항]
아직 기능을 구현하지 말고 구조만 제안할 것


---

방금 제안한 구조에서
이번 노벨티 프로젝트에 꼭 필요한 부분과
없어도 되는 부분을 구분해줘.

이번 목표는 다음 4가지 기능뿐이야.

1. 선택지 폼 제공
2. 사용자 성향 분석
3. 성향과 반대되는 랜덤한 미션 생성
4. 3D 구조의 공간과 레벨 시스템 

구조를 최대한 단순하게 유지해줘.

아직 파일은 생성하지 마.

---

지금 합의한 최소 구조를 기준으로
Frontend와 Backend 프로젝트 틀을 만들어줘.

조건:

1. 아직 기능은 구현하지 않는다.
2. Frontend와 Backend가 각각 실행되는 상태까지만 만든다.
3. 불필요한 Library를 추가하지 않는다.
4. 생성한 파일과 사용한 명령어를 마지막에 정리한다.
5. 실행에 실패하면 숨기지 말고 오류를 알려준다.

---
application.yml 파일에 DB 정보를 추가해.
오라클 접속정보는 로컬호스트를 사용하고, SID는 XE, 포트는 1521이고 아이디는 C##LG_DB , 패스워드는 12345이야. 값에 맞게 파일 내용 업데이트해.

---

현재 프로젝트를 다시 분석해줘.

아직 코드는 수정하지 마.

다음 내용을 실제 파일을 기준으로 설명해줘.

1. Frontend 프로젝트 위치
2. Backend 프로젝트 위치
3. Frontend 실행 방법
4. Backend 실행 방법
5. Spring Boot 시작 Class
6. 앞으로 Controller를 작성할 위치
7. Database 관련 설정 위치
8. Flutter 첫 화면 위치

관련 파일 경로도 같이 알려줘.

---

현재 프로젝트를 기준으로
AGENTS.md 초안을 작성해줘.

다음 내용은 반드시 포함해줘.

1. 프로젝트 목적
2. Frontend 기술
3. Backend 기술
4. Backend 기본 구조
5. REST API 사용
6. 불필요한 Library 추가 금지
7. Secret을 코드에 직접 작성하지 않기
8. 기존 파일을 대량으로 삭제하지 않기
9. 큰 변경 전에 계획 먼저 설명하기
10. 구현 후 Build 또는 테스트하기
11. 변경한 파일을 작업 마지막에 설명하기

아직 파일을 생성하지 말고 먼저 내용을 보여줘.

---

위 내용에 맞춰서 AGENTS.md 파일을 생성해줘

---

사용자에게 어떤 선택지 폼을 제공하면 성향을 파악할 수 있지만 번거롭지않고 이탈율이 적은 형태로 만들 수 있을까?

---

### 첫 번째 기능 구현 SDD 설계
이제 첫 번째 핵심 기능으로 선택지 폼 생성을 구현할거야.
위에서 너가 답변한 내용을 토대로
최종적으로 사용자가 화면에서 선택지를 다 고르면
Oracle Database에 데이터가 저장되어야해.

현재 프로젝트 구조와 AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---

그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7을 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

----
phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
sdd에 정의된 내용을 토대로 반복 진행 
---

지금까지 구현한 내용을 실행해서 볼 수 있게 어디서 어떻게 실행하면 되는지 알려줘.

---

지금까지 구현한 내용을 적절히 기능별로 분리해서 commit하고 push해줘.
커밋메세지는 한글로 작성해.

---

사용한 API를 swaggerUI를 통해 직접 테스트할 수 있게 연동해주고 테스트할 수 있는 로컬 호스트 주소로 알려줘. 그리고 앞으로 추가될 API도 동일하게 적용해.

---

프로젝트에 필요한 DB는 너가 직접 테이블을 생성해서 적용하도록 하고 지금까지 작성했던 내용과 앞으로 실행할 내용에 대해서도 DB.sql 파일에 동일하게 작성해줘.

---
앞으로 모든 디자인은 DESIGN.md를 참고해서 디자인 스타일을 적용하고 이전에 생성한 내용들도 같이 수정해.
사용할 폰트는 NotoSans-kr이야. 구글폰트에서 Noto Sans Korean 로 찾아서 적용해.

---

### 두 번째 기능 구현 SDD 설계
이제 두 번째 핵심 기능으로 사용자 성향분석을 구현 할거야.
앞서 구현한 선택지 폼에 대한 내용과 연계하여 사용자가 고른 선택지 답에 따라 성향이 정해질거야.성향 유형을 분류하고 네이밍을 해줘. 여기서 정해진 성향은 추후 랜덤 미션 생성에 사용이 될거야. 이 부분도 고려해.

사용자 성향에 대한 정보는 Oracle Database에 유저 정보로 데이터가 저장되어야해. 외에 내가 언급하지 않았지만 필요한 정보가 있다면 저장해서 관리해.

현재 프로젝트 구조와 AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---

그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7을 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

----

성향 분석을 매번 진행하는게 아닌 최초 1회 실행 하고 사용가능한 시간을 묻는 질문은 성향 분석이 끝난 후에 묻는 형태로 바꿔줘. 사용가능 시간을 묻는 질문 대신 다른 성향을 파악 할 수 있는 질문으로 변경, 앞의 내용과 중복되지 않은 새로운 유형을 분석할 수 있는 질문으로 선정해줘.

[사용자 플로우]
접속 - 최초 1회 성향 분석 선택폼 진행 - 성향 결과 + 할애 가능시간 질문 진행 - 종합 결과로 랜덤 미션 생성

추가적으로 사용자가 성향 분석을 다시 진행하고 싶다면 성향 분석 다시하기 버튼을 제공해서 진행

사용자 성향 분석 결과가 나왔을 때 사용자 성향을 프로필로 보여주기
사용자 회원가입 없이 닉네임 설정해서 로그인하고 브라우저 캐시 또는 앱 캐시로 사용자 정보 저장
닉네임은 초기 랜덤값 부여하고 사용자가 변경할 수 있도록 기능 추가
닉네임 초기값은 노벨티+숫자2자리+영문2자리 랜덤 조합으로 중복 없이 생성 
닉네임 정책으로는 중복 불가, 공백 불가,특수문자 불가, 최소 1글자 이상, 비속어 불가 

----

수정된 SSD를 적용하기 위해서 사전 설정해야하는 일이 있다면 알려줘

----

phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
...
phase 2에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
sdd에 정의된 내용을 토대로 반복 진행 
----

닉네임 관련해서 금칙어 목록 파일을 하나 생성해서 3단 구조로 관리하게 해줘
Frontend validation
+
Backend validation
+
DB constraint

----

서비스의 날짜 기준 timezone은 Asia/Seoul로 해.
사용자 식별을 위해 userID를 부여해서 관리해.
최근 완료 미션 3일간 재추천 금지
최근 노출 미션 2일간 재노출 금지
동일 카테고리 연속 추천 제한
미션 상태 로그 종류 변경 기존 선택,완료에서 GENERATED, SHOWN, SELECTED, CANCELLED, COMPLETED 로 변경, UI로 표시되는 내용은 SELECTED일 때 수행중, COMPLETED 일 때 완료로 표기  

----
사용자 성향을 고려해서 사용자가 많이 해보지 않았을 법한 행동을 미션 내용으로 생성하라고 했는데 이걸 구현할 때는 
미션에도 사용자 성향 벡터에서 사용한 값을 부여해서 사용자의 성향과 얼마나 거리가 먼지 계산 할 수 있게 해줘. 

미션 속성 추가 

id
title
description

category
difficulty

estimatedMinutes

indoorOutdoor
socialLevel
activityLevel
noveltyLevel

enabled

최초 성향 분석 후 미션 생성 시에는 기본적으로 미리 만들어둔 미션 내용 중에서 선별해서 사용해. 
5회 이상 미션을 수행해서 성향이 업데이트 된 사용자에 한해 Open AI LLM을 이용해서 추가 미션 내용을 생성하고 DB에 추가해서 다른 유사한 사용자에게도 해당 미션이 뜨게 관리해.
LLM이 미션을 새로 생성하는 주기는 미션 수행 5회를 기준으로 업데이트하게 해.

미션 생성 시에는 기존에 존재하던 미션과 중복되지 않아야하고 너무 유사하면 안돼.

----

지금까지 전체 구현 된 내용을 기준으로 정책 상 오류가 있거나 구현이 잘못된 부분은 없는지 확인하고 아직 코드는 수정하지마.

----
전체 기술 구조를 아래와 같은 형태로 맞춰.
Novelty

Frontend
│
├─ Flutter
│   ├─ 선택지 폼
│   ├─ 성향 분석 UI
│   ├─ 미션 UI
│   ├─ 프로필
│   ├─ 설정
│   └─ 3D World 화면
│
└─ Three.js
    └─ 3D World Renderer
        ├─ GLB Load
        ├─ Camera
        ├─ Lighting
        ├─ Object placement
        ├─ Level model switching
        └─ Simple animation


Backend
│
└─ Spring Boot
    ├─ User
    ├─ Personality
    ├─ Mission
    ├─ Mission Completion
    └─ World / Progression


Database
│
└─ Oracle
    ├─ USER
    ├─ PERSONALITY
    ├─ MISSION
    ├─ USER_MISSION
    ├─ WORLD_OBJECT
    ├─ WORLD_OBJECT_LEVEL
    └─ USER_WORLD_OBJECT


3D Assets

Blender / Asset 제작
↓
GLB
↓
Flutter bundle 또는 CDN
↓
Three.js

----

PROJECT_STATUS.md를 하나 만들고 핵심 기능 별로 SDD 진행상태에 대해 관리해서 혼선을 일으키지 않도록 해.
SPEC.md를 생성해서 현재까지 정의된 프로젝트 내의 정책과 규칙들을 정리하고 새로운 내용을 구현하기 전에 AGENT.md, SDD Phase와 더불어 기존 프로젝트가 어떻게 짜여져있는지 참고하게 해. 새로운 규칙이나 스펙이 정의되면 spec.md에 업데이트해.

----
### 두 번째 기능 구현 SDD 재설계
두 번째 핵심 기능 SDD에 따라 구현 도중에 많은 내용이 변경 되었기 때문에 두번째 핵심 기능에 대한 SDD를 재정의하고 다시 설계해줘. 이번 단계에서 미션생성에 대한 내용은 제외하고 성향 분석에 대해서만 진행해. 성향 분석 단계가 끝나고 나서 미션에 대한 기능 구현을 진행할거야.

----

SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑하고 난 후 
phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.

----

지금까지 구현한 내용을 적절히 기능별로 분리해서 commit하고 push해줘.
커밋메세지는 한글로 작성해.

-----

### 세 번째 기능 구현 SDD 설계
이제 세 번째 핵심 기능으로 성향과 거리가 먼 랜덤한 미션 생성을 구현 할거야.
사용자 성향을 고려해서 사용자가 많이 해보지 않았을 법한 행동을 미션 내용으로 생성해줘. 

사용자가 여러개의 미션중 하나를 선택해서 수행할 수 있는 형태로 만들어. 
하루에 수행하는 미션은 한개로 고정해. 따로 설정을 제공해서 수행할 미션 수를 선택할 수 있게 해줘. 

생성되는 미션은 큰 카테고리로 종류를 분류할 수 있게 해, 수행한 미션의 카테고리에 따라 추후 3D 공간에 배치된 물품이 성장하게 될거야. 
예를 들면 운동 , 음식 , 문화 등 앞서 선택지 폼에서 물어본 활동 같이 말이야. 

여러개 미션 목록에서 선택한 미션은 취소하거나 변경할 수 있어. 선택해서 수행중인 미션만 상단에 보이게 하고 변경 버튼을 두어 기존 미션 목록에서 다른 미션으로 바꿀 수 있게 해. 

사용자가 수행한 미션 내용에 대한 정보를 저장하고 성향에 업데이트를 적용해서 어떤 카테고리의 미션을 주로 수행했는지에 대한 정보를 추가해줘.

생성된 미션에 대한 정보와 수행여부, 미션상태는 Oracle Database에 데이터가 저장되어야해. 외에 내가 언급하지 않았지만 필요한 정보가 있다면 저장해서 관리해.

현재 프로젝트 구조와 spec.md, AGENTS.md를 먼저 확인해줘.

그리고 다음 흐름이 완성되도록
구현 작업을 가능한 작은 단위로 나눠줘.

Frontend
→ REST API
→ Spring Boot
→ Oracle
→ Response
→ Frontend

각 단계에서 생성하거나 수정할 가능성이 있는 파일도 알려줘.
아직 코드는 수정하지 말고 SDD(사양주도개발) 기반으로 설계부터 수행해.

---


그럼 SDD를 토대로 어떤 순서대로 개발을 할 것인지 phase를 나누어서 브리핑해줘

---

phase 0~7 진행할건데, 내가 사전에 미리 해야할 작업이나 세팅할 값이 있는지 알려줘

----
---- 여기까지 26.08.19 진행 ----
----

phase 0에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
...
phase 7에 해당하는 내용을 구현하고 문제가 없는지 검증도 함께 진행해.
정상 시나리오뿐 아니라 해당 Phase에서 발생 가능한 주요 실패 시나리오도 테스트해.
sdd에 정의된 내용을 토대로 반복 진행 

----

지금까지 구현한 내용을 적절히 기능별로 분리해서 commit하고 push해줘.
커밋메세지는 한글로 작성해.

----

현재까지 구현된 노벨티 프로젝트를 전체적으로 점검해줘.

아직 코드를 수정하지 마.

AGENTS.md, DESIGN.md, 각 기능의 SDD, DB.sql과 실제 Frontend/Backend 코드를 모두 확인한 뒤 분석해.

현재까지 구현 대상은 다음 3개 핵심 기능이야.

1. 선택지 폼
2. 사용자 성향 분석
3. 사용자 성향을 기반으로 한 미션 추천/선택/완료

다음 단계에서는 4번째 핵심 기능인 3D 공간 및 레벨 시스템을 구현할 예정이야.

아래 항목을 기준으로 현재 구조에 문제가 없는지 점검해줘.

1. 현재 구현된 기능과 미구현 기능 구분
2. SDD와 실제 구현의 차이
3. Frontend ↔ REST API ↔ Spring Boot ↔ Oracle 연결 상태
4. 사용하지 않는 Mock 데이터 또는 임시 코드 존재 여부
5. 중복 코드 또는 불필요하게 복잡한 구조
6. API Request/Response 구조의 일관성
7. 사용자 식별 방식
8. 닉네임 중복 방지 방식
9. 성향 분석 결과 저장 구조
10. 미션 추천 로직과 성향 데이터 연결 구조
11. 미션 상태 저장 구조
12. 미션 중복 추천 방지 여부
13. 하루 미션 기준 및 timezone 처리
14. 미션 카테고리와 향후 3D 공간 성장 시스템을 연결하기에 적절한 데이터 구조인지
15. DB constraint 및 데이터 무결성
16. 예외 처리
17. 앱 재실행 또는 브라우저 재접속 시 사용자 상태 복원
18. Swagger에서 현재 모든 API 테스트 가능 여부
19. Backend build/test 상태
20. Frontend build/analyze 상태

각 문제에 대해서는 아래 기준으로 분류해줘.

* 반드시 지금 수정해야 함
* 3D 기능 구현 전에 수정해야 함
* 나중에 개선 가능
* 현재 문제 없음

특히 현재 기능이 정상 동작하더라도 앞으로 3D 공간 및 레벨 시스템을 구현할 때 구조 변경을 크게 유발할 가능성이 있는 부분을 우선적으로 찾아줘.

아직 어떤 파일도 수정하지 말고 분석 결과와 수정 권장 순서만 알려줘.

-----

............. 사용량 끝남 ...............
다음주 월요일 오전 9시 40분에 갱신 예정

현재 Novelty 프로젝트의 1~3번 핵심 기능을 안정화하려고 한다.

구현된 기능:

1. 선택지 폼
2. 사용자 성향 분석
3. 성향 기반 미션 추천 / 선택 / 교체 / 완료

다음 단계에서는 3D 공간 및 레벨 시스템을 구현할 예정이므로, 그 전에 기존 기능의 **공식 실행 경로를 하나로 통일하고 불필요한 구조를 정리**한다.

먼저 `AGENTS.md`, `DESIGN.md`, `SPEC.md`, `PROJECT_STATUS.md`, 각 SDD, `DB.sql`, Frontend/Backend 실제 코드를 확인한다.

문서와 코드가 다르면 실제 코드를 우선한다.

## 목표

다음 흐름이 각각 하나의 공식 API / Service 경로만 사용하도록 정리한다.

```text
사용자 생성/복원
→ 선택지 제출
→ 성향 분석 및 저장
→ 미션 추천
→ 미션 선택
→ 미션 교체
→ 미션 완료
```

특히 **Mission 완료 경로는 하나로 통일**한다.

향후 다음 구조를 바로 연결할 수 있어야 한다.

```text
Mission 완료
→ 기존 완료 후처리
→ WorldProgressService
→ Category EXP
→ World Object 성장
```

## 반드시 점검할 내용

### 1. 레거시 및 중복 API

현재 존재하는 구형 API와 Service를 찾아 실제 사용 여부를 확인한다.

특히 다음과 같은 경로를 확인한다.

* random mission API
* `missionId` 기반 직접 상태 변경 API
* 구형 survey API
* 현재 공식 흐름과 별도로 상태를 변경하는 Service

Frontend / Backend / Test 어디에서도 사용하지 않는 레거시 코드는 제거한다.

삭제하기 어려운 호환 API는:

* Deprecated 처리
* Swagger에서 숨김
* 자체 Business Logic 제거
* 공식 Service로 위임

하도록 한다.

### 2. Mission 완료 흐름

사용자 Mission 상태 변경은 `USER_MISSION` 기준으로 통일한다.

완료 시 다음 처리가 하나의 공식 흐름에서 수행되도록 한다.

```text
USER_MISSION → COMPLETED
→ 상태 로그
→ Category 통계
→ 현재 존재하는 성향/완료 후처리
```

가능하면 동일 Transaction에서 처리한다.

같은 `userMissionId`로 완료 요청이 여러 번 들어와도:

* 완료 처리 중복 금지
* 통계 중복 증가 금지
* 후처리 중복 실행 금지

를 보장한다.

향후 World EXP도 이 Transaction에 연결할 수 있어야 한다.

### 3. Survey / Personality 역할

역할을 다음처럼 명확하게 유지한다.

```text
Survey / 선택지
= 사용자 입력 원본

Personality
= 분석 결과 및 현재 성향
```

두 Domain에서 동일 기능을 중복 처리하지 않도록 한다.

추천 로직 자체는 이번 작업에서 변경하지 않는다.

### 4. Mission Category

향후 3D World 연결 기준이 되므로 Category 값을 점검한다.

```text
Mission
→ Category
→ World Object
```

Frontend / Backend / DB에서 Category 이름이나 코드가 불일치하거나 문자열이 무분별하게 하드코딩되어 있지 않은지 확인한다.

현재 프로젝트 Convention에 맞는 하나의 Category Code 체계를 유지한다.

### 5. DB.sql

`DB.sql`을 현재 최종 Schema의 기준 파일로 정리한다.

중복된:

* TABLE
* SEQUENCE
* FK
* UNIQUE
* CHECK
* INDEX
* TRIGGER

정의를 제거한다.

목표는 다음이다.

```text
빈 Oracle DB
→ DB.sql 1회 실행
→ 현재 필요한 전체 Schema 생성
```

이미 존재하는 World 관련 Table은 이번 작업에서 확장하지 않는다.

중복이나 명백한 오류만 정리하고, 최종 World Schema는 이후 3D SDD에서 확정한다.

### 6. Swagger / API

Swagger에는 현재 공식 API만 노출되도록 한다.

최소한 다음 흐름을 Swagger에서 검증할 수 있어야 한다.

```text
사용자 생성
→ 선택지 제출
→ 성향 조회
→ 오늘의 미션
→ 선택
→ 교체
→ 완료
```

`userId`, `userKey`, `missionId`, `userMissionId`의 역할도 혼동되지 않는지 확인한다.

사용자 Mission의 선택/교체/완료 같은 상태 변경은 `userMissionId` 기준을 우선한다.

### 7. Mock / Dead Code

Runtime에서 사용되는:

* mock
* fake
* dummy
* temp
* hard-coded user
* 사용하지 않는 API Client
* 사용하지 않는 Repository / Service Method

를 찾아 정리한다.

Test fixture는 실제 필요하면 유지한다.

### 8. Build / Test

정리 후 실제로 가능한 범위에서 다음을 검증한다.

Backend:

```text
mvnw.cmd test
mvnw.cmd package
```

Frontend:

```text
flutter analyze
flutter test
flutter build web
```

실행하지 못한 테스트는 성공했다고 간주하지 말고 원인을 기록한다.

## 작업 순서

다음 순서로 진행한다.

```text
1. 현재 공식 / 레거시 흐름 분석
2. 레거시 API 및 중복 상태 변경 경로 정리
3. Mission 완료 흐름 통합
4. Dead Code 정리
5. DB.sql 중복 정리
6. Swagger / API 확인
7. Backend / Frontend Build 및 Test
8. AGENTS / DESIGN / SPEC / PROJECT_STATUS 동기화
```

대규모 리팩터링은 하지 않는다.

이번 작업에서는 다음을 하지 않는다.

* 3D 기능 구현
* World EXP 구현
* Three.js 추가
* WebView 추가
* DB 종류 변경
* Auth 시스템 변경
* 추천 알고리즘 변경
* 전체 Architecture 재설계

## 첫 번째 응답

아직 코드를 수정하지 않는다.

먼저 Repository를 분석한 뒤 다음만 알려준다.

1. 현재 공식 기능 흐름
2. 레거시 / 중복 코드 목록
3. 삭제 / 유지 / Deprecated 권장안
4. Mission 완료 현재 흐름과 목표 흐름
5. DB.sql 정리 대상
6. 예상 수정 파일
7. 작업 순서
8. Regression 위험
9. 3D 구현 전에 반드시 해결해야 하는 항목

분석 결과를 확인한 다음 실제 안정화 작업을 Phase별로 진행한다.


----


-----
# Novelty 4번째 핵심 기능 — 3D World / Growth System

현재 Novelty 프로젝트의 4번째 핵심 기능인 **3D 공간 및 World Object 성장 시스템**을 구현하려고 한다.

먼저 `AGENTS.md`, `DESIGN.md`, `SPEC.md`, `PROJECT_STATUS.md`, 기존 SDD, `DB.sql`, Frontend/Backend 실제 코드를 확인한다.

문서와 코드가 다르면 실제 구현을 우선하고, 기존 구조를 최대한 재사용한다.

불필요한 추상화나 미래 확장을 위한 구조는 만들지 않는다.

---

## 1. 목표

사용자가 Mission을 완료하면 해당 Mission Category와 연결된 3D Object가 성장하도록 구현한다.

```text
Mission 완료
→ Category 확인
→ EXP 증가
→ Object Level 계산
→ Object 외형 변경
```

3D 공간은 **고정된 아이소메트릭 디오라마 형태의 방**으로 한다.

MVP에서는 다음을 구현하지 않는다.

* Character / Movement
* Physics / NavMesh
* 자유로운 가구 배치
* 멀티플레이
* 다른 사용자 World 방문
* 전체 World Level 시스템

성장 단위는 **Category별 World Object**다.

---

## 2. 기술 구조

기본 구조는 다음으로 한다.

```text
Flutter
→ REST API
→ Spring Boot
→ Database

Flutter
→ WebView Bridge
→ Three.js
→ GLB
```

역할은 명확하게 분리한다.

### Flutter

* 앱 UI
* API 호출
* World State
* Three.js와 통신
* Level Up / Object 정보 UI

### Three.js

* 3D Renderer
* Room / Object / Camera / Lighting
* GLB Loading
* Object 교체 및 간단한 Animation

### Spring Boot

* Mission 완료
* EXP 계산
* Level 계산
* World Progress
* Transaction
* 중복 보상 방지

### Database

* 사용자별 World 성장 상태 저장

Three.js가 Backend API를 직접 호출하지 않는다.

Flutter나 Three.js에서 EXP/Level을 계산하지 않는다.

Backend와 DB를 사용자 성장 상태의 Source of Truth로 한다.

---

## 3. Category와 Object

MVP에서는 다음 관계로 한다.

```text
Mission Category : World Object = 1 : 1
```

현재 Repository에 실제 존재하는 Mission Category를 확인해서 대표 Object를 정한다.

존재하지 않는 Category는 새로 만들지 않는다.

Mission과 GLB 파일을 직접 연결하지 않는다.

```text
Mission
→ Category
→ World Object
→ Level
→ Asset Manifest
→ GLB
```

Backend는 `objectCode`, `categoryCode`, `level`, `exp` 등의 상태만 전달한다.

GLB 경로와 파일명은 Frontend Asset Manifest에서 관리한다.

DB에는 GLB 경로나 Binary를 저장하지 않는다.

---

## 4. 성장 정책

Object는 MVP 기준 약 5단계로 성장한다.

EXP는 누적 방식으로 사용한다.

Mission에 기존 난이도 값이 있다면 이를 활용하여 Backend에서 EXP를 계산하고, 별도의 `rewardExp` 필드는 필요하지 않으면 추가하지 않는다.

기본 EXP 정책은 다음을 우선 검토한다.

```text
EASY      → 10 EXP
NORMAL    → 20 EXP
CHALLENGE → 30 EXP
```

Level 기준은 초기 MVP에서 다음을 사용한다.

```text
Lv1 = 0
Lv2 = 50
Lv3 = 120
Lv4 = 220
Lv5 = 350
```

실제 Mission 난이도 코드가 다르면 현재 프로젝트 값을 기준으로 맞춘다.

---

## 5. World 데이터 구조

현재 DB를 먼저 확인한다.

중복 구조가 없다면 다음 개념을 사용한다.

```text
WORLD_OBJECT
- Object 정의
- objectCode
- category
- maxLevel

WORLD_OBJECT_LEVEL
- Object별 Level 기준
- level
- requiredExp

USER_WORLD_OBJECT
- 사용자별 성장 상태
- userId
- worldObjectId
- exp
- level
```

MVP에서는 Category : Object = 1:1을 보장한다.

사용자별 Progress는 `(userId, worldObjectId)`가 중복되지 않아야 한다.

Object의 Position / Rotation / Scale은 DB가 아니라 Frontend World Manifest에서 관리한다.

---

## 6. Mission 완료 연동

기존 Mission 완료 흐름을 확장한다.

```text
Mission Complete

→ USER_MISSION 완료 처리
→ Mission Category 확인
→ World Object 확인
→ EXP 지급
→ Level 계산
→ USER_WORLD_OBJECT 갱신
```

Mission 완료와 World EXP 지급은 가능한 한 동일 Transaction에서 처리한다.

동일 `userMissionId` 완료 요청이 반복되거나 동시에 들어와도 EXP가 두 번 지급되지 않아야 한다.

기존 Mission 완료 Service를 중심으로 유지하고, World 성장 책임은 필요하면 `WorldProgressService` 정도로만 분리한다.

과도한 Service 분리는 하지 않는다.

---

## 7. API

World 화면 진입 시 전체 상태를 한 번에 조회할 수 있는 Snapshot API를 만든다.

예:

```http
GET /api/world
```

실제 Endpoint 이름은 현재 API Convention을 따른다.

필요 데이터:

```text
objectCode
categoryCode
level
exp
nextLevelRequiredExp
```

기존 Mission 완료 API도 확장해서 World 성장 결과를 반환한다.

최소 정보:

```text
objectCode
previousLevel
currentLevel
currentExp
levelUp
```

새로운 Mission 완료 Endpoint는 만들지 않는다.

---

## 8. Flutter ↔ Three.js

Flutter가 Backend에서 World 상태를 받고 Three.js에 전달한다.

필요한 통신은 최소한 다음 정도로 한다.

```text
World 초기 상태 전달
Object Level 변경
Level Up Animation 실행
Object 선택 Event
Renderer Ready / Error Event
```

가능하면 JSON Message 기반 Bridge 하나로 통일한다.

Three.js 자체에 별도의 Business State를 만들지 않는다.

---

## 9. 3D Asset

Runtime Asset은 GLB를 사용한다.

초기에는 앱 Local Asset으로 관리한다.

Asset 파일명과 경로는 Frontend Manifest에서 관리하여 Backend와 분리한다.

기본 제작 기준:

* 모바일 Low Poly 우선
* 1 unit = 1 meter
* Pivot은 바닥 배치 기준
* Transform 적용 후 Export
* Texture 512~1024 우선
* 단순 PBR Material
* 과도한 Polygon / Material / Texture 사용 금지

Camera는 MVP에서 **Orthographic Camera + 제한된 회전/Zoom**을 우선 사용한다.

Interaction은 다음만 구현한다.

```text
Drag → 제한된 Camera 회전
Pinch / Wheel → Zoom
Object Tap → Object 정보 요청
```

---

## 10. Technical Spike를 먼저 수행

실제 DB/Backend World 기능보다 먼저 다음을 검증한다.

```text
Flutter Test Screen
→ WebView
→ Three.js
→ Room GLB
→ Test Object GLB
```

Flutter 버튼으로:

```text
Lv1 GLB → Lv2 GLB
```

교체가 가능한지 확인한다.

반드시 확인할 것:

* Flutter에서 Three.js 정상 실행
* GLB 정상 로드
* Flutter ↔ JavaScript 통신
* Object 교체
* Camera / Zoom
* 화면 종료 후 Renderer Dispose
* 화면 재진입
* 앱 Background / Foreground
* 실기기 성능 및 메모리

핵심 항목이 실패하면 실제 World 구현으로 넘어가지 말고 원인과 대안을 먼저 정리한다.

---

## 11. 구현 순서

한 번에 전체 기능을 구현하지 않는다.

다음 순서로 진행한다.

```text
Phase 0
현재 구조 분석 및 SDD 확정

Phase 1
Flutter + WebView + Three.js Technical Spike

Phase 2
World DB / Backend Domain

Phase 3
World Snapshot API

Phase 4
Mission Completion ↔ World Progress 연결

Phase 5
Flutter World State

Phase 6
실제 Three.js World Renderer

Phase 7
Backend ↔ Flutter ↔ Three.js 통합

Phase 8
Level Up UI / Animation

Phase 9
Integration / Regression Test
```

각 Phase 완료 후 Build/Test를 확인한 다음 다음 단계로 진행한다.

---

## 12. 첫 번째 작업

아직 코드를 수정하지 않는다.

먼저 Repository를 분석한 후 다음만 알려준다.

1. 현재 구조에서 재사용 가능한 부분
2. 새로 필요한 DB / Backend / Flutter / Three.js 구조
3. 현재 계획과 충돌하는 기존 코드
4. 실제 Mission Category와 World Object 매핑안
5. Technical Spike 구현 계획
6. 위 Phase 계획을 현재 Repository에 맞게 조정한 결과
7. 3D 구현 전에 추가로 해결해야 할 문제

그 결과를 확인한 뒤 Phase 1부터 구현한다.

----

Phase 0  World SDD와 Bridge·Schema 계약 확정
Phase 1  Android WebView + Three.js Technical Spike
Phase 2  World DB 및 Backend Domain
Phase 3  GET /api/world Snapshot API
Phase 4  Mission 완료와 World Progress 연결
Phase 5  Flutter World State
Phase 6  실제 Three.js Diorama Renderer
Phase 7  Backend ↔ Flutter ↔ Three.js 통합
Phase 8  Level Up UI와 Animation
Phase 9  Oracle·Android·Web 통합 회귀 테스트

flutter main 실행 시 API 주소가 설정되지 않아 사용자 정보를 불러오지 못했다는 창이 떠 이 문제 먼저 해결 하고
Phase 0 ~ 9까지 순차적으로 실행해

----

전체 구현한 내용에 대해 구현된 코드와 문서화된 내용 중에 차이는 없는지 확인하고 구현된 코드를 기준으로 문서들 내용을 업데이트해. 추가로 현재 프로젝트에서 환경 변수에 대한 값을 저장해두고 연결할 때 알아서 가져와서 사용하게 해주는 게 뭐가 있어? 없으면 생성해서 연결해줘. 매번 실행할때마다 터미널에 입력하지 않게 해줘.

---- 2026.08.20 여기까지 작업 진행

----

# 디자인 요소 수정 진행

Figma에서 레이아웃 제작 후 첨부 
첨부한 이미지처럼 레이아웃을 설정할거야.
오늘의 미션에 대한 내용을 상단으로 올리고 선택하기 버튼을 누르면 오늘 할애가능한 시간을 선택하게 할거야. 미션수를 선택할 수 있는 옵션은 제거해.

시간을 선택하면 바로 다음 화면처럼 캐러셀 UI를 적용해서 미션 카드를 선택하는 형태로 지원해. 선택 버튼을 누르면 마지막 화면 처럼 오늘의 미션 진행 상황과 진행중인 미션 내용이 크게 단독으로 보이는 형태면 돼.

성향 프로필에 대한 내용은 중간에 배치하고 관심 분야와 행동 선호에 대한 내용은 분리해서 작성해. 선호 그래프를 추가해서 어느정도 수치인지 알 수 있게 표시해줘. 그리고 미션 수행에 따라 해당 수치에 변동이 생기는것을 알 수 있게 미션 완료 시 행동 선호 변화에 대한 내용을 표시해줘.

하단에 공간이 보이게 하고 공간에 어울리는 에셋은 추후  첨부하면 적용해줘. 공간을 꾸미거나 하는 기능은 업성. 행동 선호에 매칭되는 에셋의 변화만 보여주면 돼. 현재 표기되어있는 동작에 대한 설명 문구는 평소헤 ? 모양의 도움말 버튼이다가 누르면 길게 펼쳐지는 형태로 구성해줘.

----

시작화면과 성향 선택폼에 대한 레이아웃이야. 사용중인 Design.md 에 맞게 색감이나 요소들 적용해.  시작화면에 서비스 설명에 대한 문구는 꼭 사용하도록 하고 첫 선택지 화면에서는 이전 버튼을 비활성화해서 안보이게 하고 다음 질문부터 보이게 해. 
추가로 이전에 사용했던 사용자가 들어올 경우를 대비해서 이미 사용중인 유저가 들어올 수 있는 경로에 대한  고민를 해줘.

----

# 알고리즘 수정 및 테스트 진행 

 현재 노벨티 프로젝트의 미션 추천 구조를 개선해줘.
먼저 기존 AGENTS.md, DESIGN.md, 관련 SDD, DB.sql, Backend/Frontend 코드를 확인하고 현재 구조를 최대한 유지한 상태에서 구현해. 기존 기능을 불필요하게 재설계하거나 중복 구현하지 마.
현재의 동일 미션 3일 내 재노출 금지 정책은 유지하되, 문구만 다른 유사 미션이 반복되는 문제를 해결한다.
추천의 핵심 기준은 다음과 같다.
같은 미션을 반복하지 않는 것이 아니라, 최근 사용자가 경험한 행동 패턴과 다른 경험을 추천한다.
각 미션이 추천 다양성 계산에 사용할 수 있도록 기존 DB 구조를 확인한 후 필요한 필드를 추가한다.
필요한 개념:
- category
- actionType
- environment
- socialLevel
- physicalLevel
- creativityLevel
- unpredictability
- comfortZoneDistance
- durationLevel
- costLevel
- tags
기존 필드와 중복되는 값이 있다면 새로 만들지 말고 재사용한다.
추천 후보 생성 시 다음 정책을 적용한다.
- 동일 미션 3일 내 재노출 금지
- 현재 사용자 조건에서 수행 불가능한 미션 제외
- 이미 수행 중인 미션 등 기존 제외 정책 유지
후보 미션에 다음 요소를 이용해 점수를 계산한다.
- 사용자 성향과 반대되는 활동 적합도
- Novelty
- 최근 카테고리 다양성
- 최근 수행하지 않은 활동에 대한 Exploration Bonus
- 사용자 상황/조건 적합도
동시에 다음 패널티를 적용한다.
- 최근 미션과의 유사도
- 최근 반복된 category
- 최근 반복된 tag / actionType
- 동일하거나 거의 동일한 경험 패턴
초기 가중치는 상수/config로 분리해서 이후 쉽게 조정할 수 있도록 한다.
최근 7일 또는 최근 N개 완료 미션을 기준으로 후보를 평가한다.
최근에 많이 수행한:
- category
- actionType
- environment
- tags
- social/physical/creativity 특성
과 유사할수록 추천 점수를 낮춘다.
반대로 최근 경험하지 않은 활동 특성에는 가산점을 준다.
최근 활동일수록 유사도 패널티가 더 크게 적용되도록 recency weight를 사용한다.
현재 프로젝트에서 Embedding 사용이 현실적으로 가능한 구조인지 먼저 확인한다.
가능하다면:
- 미션 제목 + 설명 + 핵심 태그를 embedding으로 저장
- 최근 미션과 후보 미션의 cosine similarity 계산
- 매우 유사한 미션은 후보에서 제외
- 어느 정도 유사한 미션은 점수 감점
Embedding을 추가할 경우 기존 Supabase/PostgreSQL 구조와 호환되는 방식으로 구현하고, 가능하면 pgvector를 활용한다.
단, Embedding 때문에 전체 구현 복잡도가 지나치게 증가한다면 먼저 메타데이터 기반 유사도 로직을 완성하고 Embedding은 확장 가능한 인터페이스로 분리한다.
점수가 높은 미션을 단순히 상위 3개 반환하지 않는다.
최종 추천 3개끼리도 서로 비교하여:
- category
- actionType
- tags
- semantic similarity
가 지나치게 겹치지 않도록 선택한다.
예:
나쁜 결과:
1. 새로운 카페 방문
2. 새로운 음식점 방문
3. 새로운 산책길 탐색
좋은 결과:
1. 새로운 장소 방문
2. 평소 하지 않던 사람에게 연락
3. 새로운 창작 활동
즉 사용자가 선택지를 보는 순간부터 서로 다른 경험이라고 느낄 수 있어야 한다.
가능하면 기존 로그 구조를 활용하여 다음 상태를 구분한다.
- exposed
- selected
- rejected/skipped
- completed
특정 활동 유형을 반복적으로 선택하지 않는다고 즉시 제거하지 않는다.
반복 거부되는 영역은:
- 추천 빈도를 낮추거나
- 동일한 경험 영역에서 난이도가 낮은 미션을 우선 추천한다.
comfortZoneDistance를 이용해 새로움과 부담도를 구분할 수 있도록 한다.
- 기존 1~3 핵심 기능의 공식 흐름을 깨지 않는다.
- 기존 추천 로직을 확인하고 가능한 한 확장 방식으로 구현한다.
- 중복 service/repository/model을 만들지 않는다.
- magic number를 최소화하고 추천 가중치와 threshold는 config 또는 상수로 분리한다.
- 추천 점수 계산은 테스트 가능한 독립 로직으로 구성한다.
- DB 변경이 필요하면 migration/DB.sql/관련 문서를 함께 수정한다.
- API response 변경이 필요하면 Frontend 영향까지 확인한다.
- 필요하지 않은 UI 변경은 하지 않는다.
최소한 다음 케이스를 테스트한다.
1. 동일 미션이 3일 내 다시 추천되지 않는지
2. 문구만 다른 유사 미션이 연속 추천되지 않는지
3. 최근 7일간 많이 수행한 category의 점수가 낮아지는지
4. 최근 경험하지 않은 category가 우선될 수 있는지
5. 최종 추천 3개가 서로 과도하게 유사하지 않은지
6. 기존 사용자 성향 기반 추천이 유지되는지
7. 기존 미션 선택/완료 흐름이 깨지지 않는지
구현 완료 후 다음 내용만 정리해서 보고해줘.
- 변경한 파일
- DB 변경사항
- 추천 알고리즘 흐름
- 사용한 score/penalty 기준
- 테스트 결과
- 기존 SDD와 달라져 추가 수정이 필요한 문서

----

현재 구현된 노벨티 프로젝트의 **미션 추천 알고리즘을 검증해줘.**

이번 단계에서는 **코드를 수정하지 말고 분석과 테스트만 수행해.**

먼저 AGENTS.md, DESIGN.md, 관련 SDD, DB.sql, Backend/Frontend 코드와 최근 변경된 추천 로직을 확인해.

## 검증 목표

현재 추천 시스템이 다음 원칙을 실제로 만족하는지 확인한다.

> 동일한 미션뿐 아니라 최근 사용자가 경험한 행동 패턴과 유사한 미션의 반복을 줄이고, 매번 서로 다른 경험을 제공한다.

## 1. 구현 상태 확인

다음 기능이 실제 코드에 구현되어 있는지 확인해.

* 동일 미션 3일 내 재노출 금지
* 최근 7일 또는 최근 N개 미션 이력 활용
* category 반복 패널티
* tag / actionType 반복 패널티
* 최근 경험하지 않은 활동에 Exploration Bonus
* 사용자 성향 기반 추천 유지
* 미션별 novelty / diversity 관련 점수 계산
* 최종 추천 3개 간 유사도 검사
* Embedding 또는 metadata 기반 유사도 계산
* 추천 가중치 및 threshold가 상수/config로 관리되는지

각 항목을 `구현됨 / 부분 구현 / 미구현`으로 구분해줘.

## 2. 추천 흐름 검증

실제 코드 기준으로 추천 과정이 어떻게 동작하는지 추적해.

아래 형태로 정리해줘.

```text
Candidate 생성
→ Hard Filter
→ 사용자 성향 점수
→ Novelty / Diversity 계산
→ 최근 활동 유사도 패널티
→ Exploration Bonus
→ 후보 정렬
→ 후보 간 다양성 검사
→ 최종 3개 반환
```

실제 구현과 다른 부분이 있다면 표시해.

## 3. 추천 시뮬레이션

가능하면 테스트 데이터 또는 임시 데이터를 사용해 최소 **30일간의 사용자 미션 추천을 시뮬레이션**해.

하루에 최종 선택지 3개를 생성하고, 임의로 1개를 선택/완료한 것으로 가정해 다음 날 추천에 반영해.

검증 항목:

* 동일 미션 반복
* 의미상 유사한 미션 반복
* 동일 category 편중
* 동일 actionType 편중
* 동일 tag 편중
* 실내/실외 활동 편중
* 사회/개인 활동 편중
* 신체/창작/탐색 등 경험 특성 편중
* 최종 선택지 3개끼리의 유사성

## 4. 반복 패턴 탐지

다음처럼 문장은 다르지만 실제 경험은 유사한 미션이 반복되는지 확인해.

```text
새로운 카페 방문
새로운 음식점 방문
새로운 장소 찾아가기
새로운 산책길 걷기
```

Mission ID 중복 여부뿐 아니라 category, actionType, tag, metadata 및 semantic similarity 기준으로 판단해.

## 5. 추천 점수 검증

대표적인 후보 미션 몇 개를 선정해서 실제 점수 계산 과정을 보여줘.

예:

```text
Mission A

PersonalityScore:
NoveltyScore:
DiversityScore:
ExplorationBonus:
SimilarityPenalty:
RepetitionPenalty:

FinalScore:
```

어떤 이유로 추천 순위가 올라가거나 내려갔는지 확인할 수 있어야 한다.

## 6. 추천 선택지 다양성 검증

최종 추천된 3개 미션이 서로 충분히 다른지 확인해.

특히 아래와 같은 결과가 발생하는지 검사해.

```text
1. 새로운 카페 방문
2. 새로운 음식점 방문
3. 새로운 산책길 탐색
```

이런 경우 서로 다른 mission이더라도 다양성 실패로 판단해.

## 7. 사용자 행동 이력 검증

다음 상태가 추천 로직에 적절히 반영되는지 확인해.

* exposed
* selected
* skipped/rejected
* completed

특히 사용자가 특정 활동을 여러 번 선택하지 않았을 때:

* 완전히 추천 대상에서 제거되는지
* 추천 빈도만 감소하는지
* 난이도가 낮은 유사 활동으로 조정되는지

확인해.

## 8. 기존 기능 회귀 테스트

추천 알고리즘 변경으로 기존 핵심 기능이 깨지지 않았는지도 확인해.

* 선택지 폼
* 사용자 성향 분석
* 성향 기반 미션 추천
* 미션 선택
* 미션 완료
* 기존 DB/API 흐름

## 최종 보고

코드는 수정하지 말고 아래 순서로 결과만 정리해줘.

1. 구현 상태표
2. 실제 추천 알고리즘 흐름
3. 30일 시뮬레이션 결과
4. 발견된 반복/편중 문제
5. 추천 점수 계산 예시
6. 기존 기능 회귀 테스트 결과
7. 중요도 기준 문제 목록

   * Critical
   * High
   * Medium
   * Low
8. 수정이 필요한 파일 목록
9. 수정이 필요한 SDD / DESIGN / DB 문서
10. 다음 수정 단계에서 처리해야 할 작업

문제가 발견되더라도 이번 단계에서는 직접 수정하지 말고, **원인과 수정 방향까지만 제시해.**

----

현재 노벨티 프로젝트의 추천 알고리즘 검증 결과는 다음과 같다.
SIM_DAYS=30
COMPLETED=30
EMPTY_DAYS=0
DAYS_WITH_LT3=11
SAME_ID_WITHIN_3_DAYS=0
RECENT_SIMILAR_COMPLETED_GE_082=0
INTRA_PAIR_GE_075=0
MAX_INTRA_SIM=0.495
CATEGORY_DUP_DAYS=5
ACTION_DUP_DAYS=0
동일 미션 3일 제한, 최근 유사 미션 차단, 선택지 간 다양성은 정상적으로 동작한다.
현재 핵심 문제는 30일 중 11일에서 추천 후보 3개를 확보하지 못했다는 점이다.
기존 추천 알고리즘을 불필요하게 재설계하지 말고 다음 사항만 보완해줘.
1. 기존 구조 확인
먼저 다음을 확인한다.
AGENTS.md
DESIGN.md
미션 추천 관련 SDD
DB.sql
Mission Entity / Repository / Service
추천 Score 및 Diversity 관련 코드
기존 테스트 및 시뮬레이션 코드
기존 필드와 로직을 최대한 재사용하고 중복 구조를 만들지 않는다.
2. Mission Pool 확장
추천 후보로 사용할 미션을 최소 200개 수준으로 확보한다.
카테고리는 특정 영역에 편중되지 않도록 분산한다.
주요 카테고리:
MOVEMENT
CREATIVE
FOOD
LEARNING
SOCIAL
OUTDOOR
ORGANIZING
CULTURE
각 미션에는 현재 추천 시스템에서 사용하는 metadata를 연결한다.
가능한 기존 필드를 재사용한다.
예:
category
actionType
environment
socialLevel
physicalLevel
creativityLevel
unpredictability
comfortZoneDistance
durationLevel
costLevel
tags
아래에 별도로 제공한 200개 Mission Seed 데이터를 기준으로 추가한다.
기존 DB에 존재하던 미션 후보는 제거하고 새로운 데이터로 삽입한다.
3. 동일 미션 장기 재노출 패널티
기존 정책:
최근 3일 동일 Mission → Hard Filter
는 유지한다.
3일 이후 즉시 일반 후보로 복귀시키지 말고 최근 완료 이력에 따라 패널티를 적용한다.
기본 정책:
0~3일   → Hard Filter
4~7일   → 매우 높은 반복 패널티
8~14일  → 높은 반복 패널티
15~30일 → 낮은 반복 패널티
30일 초과 → 일반 후보
정확한 값은 config/constants로 분리한다.
기존 RecentSimilarityPenalty와 중복 계산되지 않도록 역할을 구분한다.

아래와 같은 형식으로 된 200개 후보 질문풀 첨부  
Seed | Category | ActionType | Environment | Social | Physical | Creativity | Unpredictability | Comfort | Mission | Tag
----

날짜와 연관해서 정책 추가할게
하루 미션 및 추천 이력의 날짜 판정은 사용자의 설정 timezone을 기준으로 한 local date를 사용한다. 동일 local date 내에서는 최초 생성된 추천 목록을 재사용하며 날짜가 변경되면 새로운 추천 주기를 시작한다.  현재는 Asia/Seoul  를 기준으로 사용한다

----

시작 화면에서 닉네임 대신 사용자 아이디 및 비밀번호 입력하는 형태로 변경, 회원가입 추가, 닉네임은 초기 랜덤값으로 배정하고 추후 변경 가능하게 수정. 성향 분석 결과 타이틀이 design.md에 맞는지 확인하고 어울리게 수정, 미션 선택 후 화면과 완료 화면 첨부한 디자인 레이아웃에 맞게 재 수정 . 나의세계에서 도움말 버튼 동작 안하는 것 확인 후 수정. 3D 공간에 성향과 매칭되는 모델링 배치, 단계별로 성장하는지 확인

----

오늘의 미션에서 미션 선택 후 변경 버튼 삭제, 취소만 남겨두기.
오늘의 미션 옆에 하루 한개 텍스트 삭제 , 캐러셀 마우스 드래그 동작 가능하게 수정. 웹앱 형태로 빌드할 것임. 해당 사항 반영해서 기능 수정

----

미션에 따른 성향 변동은 매 미션 완료 마다 반영하고 추천되는 미션도 매일 갱신될 때 마다 반영해서 미션을 바꿔줘. 그리고 미션 5회  완료 시 사용자의 현재 성향에 따라 다음날에 기존에 존재하는 미션이 아닌 신규로 생성되는 미션을 만들어서 추가 반영해. 그리고 미션 완료로 인한 성향 변동이 있을 경우 프로필에서 볼 수 있게 표기하고 어떻게 동작하는지에 대한 내용을 텍스트로 적지마. 나의세계에서 도움말 버튼은 아이콘만 있는 버튼으로 하고 버튼 클릭 시 설명 텍스트가 뜨는 형태로 바꿔. 그리고 설명 텍스트는 5초후에 자동으로 페이드 아웃되고 다시 도움말 아이콘 버튼만 보이게 해

----

전반적으로 디자인이 DESIGN.md 에 맞게 되어있는지 확인하고 반영해

----

동작에 영향을 끼치지 않고 사용하지 않는 불필요한 코드나 파일이 있으면 제거해.
지금까지 구현된 내용을 문서들에 반영해서 업데이트하고 기능별로 나눠서 commit하고 push까지 진행해. 커밋 메세지는 한글로 작성해.

----

현재 사용자 플로우에서 시간을 선택하는 옵션을 제공하지 않고 대신 미션 선택지를 최대 5개까지 보여주는 형태로 로직을 변경해줘

----

실제 미션 pool에서 1시간 이상 소요하는 미션이 존재해?

----

현재 노벨티 미션풀에서 사용중인 메타데이터는 뭐가 있지?

----

미션풀에 새로운 미션을 추가한다고 할때 어떤 컬럼을 사용해야하는지도 알려줘

----
readme 파일을 현재 프로젝트 상태에 맞게 업데이트 하고 이 프로젝트를 처음 보는 사람이 이해 할 수 있도록 설명도 추가해. 가독성을 고려하고 시인성 있는 구조로 작성해야해.영상이 필요하다면 어떤  내용이 포함된 내용이 있으면 좋을지 알려줘.
현재 구현된 코드를 중심으로 프로젝트 스펙, 상태 등 문서들 업데이트 해줘.

----
전달할 에셋에서 wall과 floor가 이름에 포함된 에셋이 기본 room 골조야.
고요한 몰입가	익숙하고 조용한 공간에서 혼자 집중  -> classroom\_2
아늑한 탐색가	편안한 공간을 중심으로 새로운 연결을 시도 -> art\_gallery\_4
다정한 아지트지기	편안한 공간에서 사람들과 교류 -> cafe\_5
유연한 독립가	장소에 얽매이지 않고 혼자만의 리듬 유지 -> music\_store\_20
균형 조율가	혼자와 함께, 실내와 실외를 상황에 맞게 조율 -> flower\_shop\_26
열린 연결가	다양한 장소에서 사람들과 자연스럽게 어울림 -> Theatre\_32
독립 탐험가	야외에서 혼자 발견하고 경험 -> Gym\_25
자유로운 개척자	야외 활동과 필요할 때의 사회적 연결을 선호 -> bookshop\_7
활기찬 연결가	야외에서 사람들과 함께 움직이며 활력 획득-> stadium\_40

성향 타입 별로 해당 에셋명인 룸과 연결하고 성향에 따라 연결되는 에셋은 다음에 이어서 말해줄게. 아직 에셋은 집어넣지 않았는데 해당 내용만 반영해둬

----
glb로 변환한 파일 첨부 
각 성향에 해당하는 명칭에 맞게 기본 룸을 연결해주고 제대로 나오는지 확인해. 그리고 내부에 포함된 floor와 wall이 포함된 게 기본 룸 구조야. 그외 나머지 장식 요소는 성향과 연결해서 하나씩 늘어나는 것처럼 보이게 하고 싶어. 너가 적절하게 분배해서 단계별로 어떤 성향이 몇개가 보이게 할지 정해줘.

-----

테스트로 성향 레벨이 만렙일 때 모습까지 변화하는 것을 보고 싶어. 테스트용 UI GLB를 하나 생성해주고 성향 레벨을 일괄 1레벨씩 올리는 용도로 만들어주고 추후 배포할 떄는 포함되지 않게 분리해줘

----

배포를 위한 준비를 할거야. 배포는 웹앱 형태로 해서 웹 사이트와 앱 apk 2가지 다 지원할 예정이야. 어떤 사전 작업이 필요한지 현재 프로젝트 상태를 전체적으로 확인한 후 계획을 세워줘.

----

현재 이 프로젝트를 운영에 배포하려고 한다. 배포는 coolify를 이용하고, db는 oracle이 아닌 supabase의 postgre 환경을 쓸 것이다.


1. 가장 먼저 해당 oracle의 테이블과 데이터를 모두 확인해서 postgre로의 이관이 충분한지 먼저 조사할 것.

-----

좋아 그럼 이관을 진행하자. 이를 위해 내 postgre 접속 정보를 공유할게 (어차피 테스트용이야) 그 다음에 네가 이관을 하기 위한 코드 변경을 수행한 다음 실제 이관을 진행하고, 최종 결과를 보고해

[postgre 접속 정보]
postgresql://postgres.tvzfnchrfebedoviiiir:[YOUR-PASSWORD]@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres
USER-PASSWORD : zO4Rojhg2l0FQoS3

host=aws-0-ap-northeast-2.pooler.supabase.com
port=5432
database=postgres
user=postgres.tvzfnchrfebedoviiiir

( 해당 정보는 supabase에서 신규 프로젝트 생성 후 Connect -> Direct -> Session pooler -> URI 진입 후 하단에 shared pooler에 적힌 pw와 Connection parameters들을 사용 )

-----

최초 실행과 재실행 시 초반 스플래쉬 애니메이션과 로딩이 좀 오래 걸리는데 최적화 문제가 있는 구간이 있는지 확인해주고 혹시 시작 애니메이션이 일정 초 이상 실행되게 되어있지 않은지도 같이 확인해.

----

1. 고정 스플래시를 제거하고 실제 초기화 시점을 기준으로 실행하게 해줘.
2. 지정한 룸만 로드 하는 형태로 진행

4) glb 로더 캐시 적용해서 실제 필요한 룸만 로드하게 해줘
5) 폰트도 번들에 포함시켜서 네트워크 의존성을 제거하자.

----

readme 파일과 연관된 참고 문서들 현재 프로젝트 상태에 맞게 업데이트 해줘

----

api에서 사용하지 않는 하루 수행 시간과 수행할 미션수에 대한 설정에 대한 값들을 제거하고 코드에도 반영해줘.

----

전체적으로 정상 동작하는지 유저 3명이 각각 미션 수행 20회 진행해서 5회마다 갱신되는 것까지 정상 동작하고 프론트에도 반영이 되는지 확인하는 분량으로 진행하는 임시 하네스 생성해서 확인하고 문제가 생긴 부분이 있다면 수정해. 그리고 임시로 만든 내용은 테스트가 끝나면 제거하고 모든 변경사항을 git에 commit하고 push 까지 진행해. 커밋 메세지는 한글로 작성해

---- 20260826 여기까지 진행해두고 마무리 

----

# coolify 배포 본격적으로 진행



----
